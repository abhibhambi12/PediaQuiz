{
  "name": "pediaquiz",
  "private": true,
  "workspaces": [
    "workspaces/frontend",
    "workspaces/functions",
    "workspaces/types"
  ],
  "scripts": {
    "build": "npm run build --workspace=@pediaquiz/types && npm run build --workspace=@pediaquiz/functions",
    "dev": "npm run dev --workspace=@pediaquiz/frontend",
    "clean": "rm -rf node_modules && find . -name 'node_modules' -type d -prune -exec rm -rf '{}' + && find . -name 'package-lock.json' -type f -delete && npm cache clean --force",
    "firebase": "firebase",
    "deploy": "firebase deploy",
    "deploy:functions": "firebase deploy --only functions",
    "deploy:functions-fixed": "bash ./deploy-functions.sh"
  },
  "devDependencies": {
    "firebase-tools": "^13.13.2",
    "typescript": "^5.4.5"
  }
}