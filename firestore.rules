rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // You are the only user, so if you are authenticated, you have full access.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}